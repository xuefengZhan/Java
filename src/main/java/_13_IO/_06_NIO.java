package _13_IO;

import javafx.scene.shape.Path;
import org.junit.Test;

import java.nio.file.Paths;

public class _06_NIO {
    //io：阻塞式IO
    //NIO:非阻塞IO

    @Test
    public void test01(){
        //interface path
        //todo path接口类似于file
        // 获取Path对象
        // get(String url)
        // get("父目录"，“子目录”，“子子目录”)；不用写//
        //Paths.get()
    }
}
